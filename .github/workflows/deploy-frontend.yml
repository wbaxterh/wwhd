name: Deploy Frontend to Amplify

on:
  push:
    branches: [master]
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy-frontend.yml'
  workflow_dispatch:

env:
  AWS_REGION: us-west-2

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    permissions:
      id-token: write
      contents: read
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: GitHubActions-Frontend

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test -- --passWithNoTests

      - name: Build application
        run: |
          npm run build
          echo "✅ Frontend build completed successfully!"
        env:
          NEXT_PUBLIC_API_URL: http://wwhd-alb-1530831557.us-west-2.elb.amazonaws.com

      - name: Deploy to S3 + CloudFront (Alternative to Amplify)
        run: |
          echo "📦 Frontend built successfully!"
          echo "🚀 Manual deployment to Amplify or S3 can be done later"
          echo "💡 For now, frontend works locally at http://localhost:3000"
          echo "🎯 Backend is deployed and accessible at: http://wwhd-alb-1530831557.us-west-2.elb.amazonaws.com"